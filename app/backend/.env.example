SUPABASE_URL=https://gachxhquivfvwejytsbb.supabase.co
# service_role secret key (required). Used to bypass RLS; the backend enforces
# per-organization scoping itself via ScopedSupabaseClient.
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here
# anon/publishable key (required). Used to verify user access tokens against
# the Supabase Auth API (GoTrue) and to call SECURITY DEFINER RPCs as the user.
SUPABASE_ANON_KEY=your_supabase_anon_key_here
ANTHROPIC_API_KEY=sk-ant-...
BREVO_API_KEY=xkeysib-...
SENDER_EMAIL=noreply@yourdomain.com
SENDER_NAME=Kairos CRM
GOOGLE_API_KEY=AIza...
FRONTEND_URL=https://polkorp.com/dashboard
BACKEND_URL=https://api.polkorp.com
# Render API key for automation (deploy triggers, service status, etc.)
RENDER_API_KEY=rnd_...
# Comma-separated list of allowed CORS origins for production.
ALLOWED_ORIGINS=https://polkorp.com,https://www.polkorp.com,https://kairos.polkorp.com
